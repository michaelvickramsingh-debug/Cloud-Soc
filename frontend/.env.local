# Local development mode: run the dashboard against the local Flask backend.
# This keeps the app self-contained for simulation, Prowler ingest, and
# live alert/log timeline validation during development.
VITE_API_URL=http://127.0.0.1:5001/api
VITE_SOCKET_URL=http://127.0.0.1:5001
