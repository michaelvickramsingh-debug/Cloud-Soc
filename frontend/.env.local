# Points the local dashboard at the live AWS-deployed backend
# instead of a local Flask instance, so real CloudTrail ingestion
# and simulations both hit the same production database.
VITE_API_URL=https://4d6spw8ar6.execute-api.us-east-1.amazonaws.com/api
VITE_SOCKET_URL=https://4d6spw8ar6.execute-api.us-east-1.amazonaws.com
